# Backlog

- Track `smoke:microsoft-first-run-enqueue-source` as a pre-existing baseline note; do not treat it as a Phase 2 production blocker.
- Revisit Vercel CLI devDependency once Vercel patches the dependency graph; keep using `npx --yes vercel ...` until audit stays clean.
