# Template Files

Place downloadable compliance template files here.
Filename convention: {template-id}.{docx|xlsx}
Example: ropa-gdpr.docx

When a file is added, update the corresponding template entry in lib/marketing/templates.ts:
- Set status: 'available'
- Set filePath: '/templates/{filename}'
