import { PlaceholderPage } from "@/components/app/placeholder-page";

export default function TeamPage() {
  return (
    <PlaceholderPage
      title="Tým"
      description="Přístupové revize, uživatelé a evidence bezpečnostního i AI literacy školení."
      items={["access reviews", "training log", "role assignments"]}
    />
  );
}
